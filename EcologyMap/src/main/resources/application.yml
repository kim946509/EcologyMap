spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mysql
    username: m1nddoong
    password: 4761425
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    #전송 쿼리 확인
    show-sql: true
    properties:
      hibernate:
        ddl-auto: update # 애플리케이션 실행 시 데이터베이스 스키마 자동 업데이트
        format_sql: true
      show-sql: true # 실행되는 SQL 쿼리를 로그로 출력
    # 테이블 생성 후에 data.sql 생성
    defer-datasource-initialization: true
